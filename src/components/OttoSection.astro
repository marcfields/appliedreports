---
import { site } from "../site";
import SectionShell from "./SectionShell.astro";

const { ottoIntro } = site;
---

<section id="otto" class="scroll-mt-6 bg-tone" aria-labelledby="otto-intro-heading">
  <SectionShell>
    <h2 id="otto-intro-heading" class="heading-h2 text-type whitespace-pre-line">
      {ottoIntro.heading}
    </h2>
    <hr class="section-divider" />
    <p class="section-lead text-type">
      {ottoIntro.body}
    </p>

    <div class="section-steps">
      {
        site.productSteps.map((step) => (
          <article
            class:list={[
              "grid items-center gap-10 lg:grid-cols-2 lg:gap-16",
              step.iconPosition === "right" && "lg:[&>div:first-child]:order-2",
            ]}
          >
            <div
              class:list={[
                "flex w-full justify-center",
                step.iconPosition === "right" ? "lg:justify-end" : "lg:justify-start",
              ]}
            >
              <img
                src={step.icon}
                alt=""
                class="w-full max-w-[28rem] object-contain"
                width={448}
                height={448}
                loading="lazy"
              />
            </div>
            <div
              class:list={[
                "flex flex-col justify-center",
                step.iconPosition === "right" && "lg:text-right",
              ]}
            >
              <h3 class="heading-h3-step text-type">
                <span class="text-mark">{step.id}</span>
                <span> {step.title}</span>
              </h3>
              <p
                class:list={[
                  "table-feature mt-4 text-type",
                  step.iconPosition === "right" && "lg:ml-auto",
                ]}
              >
                {step.summary}
              </p>
              <p
                class:list={[
                  "table-body mt-4 max-w-[59rem] text-type",
                  step.iconPosition === "right" && "lg:ml-auto",
                ]}
              >
                {step.body}
              </p>
            </div>
          </article>
        ))
      }
    </div>
  </SectionShell>
</section>
