---
import { site } from "../site";
import SectionPanel from "./SectionPanel.astro";
import SectionShell from "./SectionShell.astro";

const { builtFor } = site;
---

<SectionPanel align="left" panelClass="text-type" aria-labelledby="built-for-heading">
  <SectionShell>
    <h2 id="built-for-heading" class="heading-h2 text-type">
      {builtFor.heading}
    </h2>
    <hr class="section-divider" />

    <ul class="section-content grid gap-12 sm:grid-cols-3 sm:gap-8">
      {
        builtFor.items.map((item) => (
          <li class="flex flex-col items-center text-center">
            <img
              src={item.icon}
              alt=""
              class="size-[9.375rem] object-contain"
              width={150}
              height={150}
              loading="lazy"
            />
            <h3 class="table-header mt-8 text-type">
              {item.title}
            </h3>
            <p class="table-body mt-4 text-type whitespace-pre-line">
              {item.body}
            </p>
          </li>
        ))
      }
    </ul>
  </SectionShell>
</SectionPanel>
